<!--     <div class="side-list">

      <% @playlists.each do |playlist| %>
      <p class="list-side"><%= link_to playlist.name, gigs_path(playlist_id: playlist.id) %></p>
      <% end %>

      <% @top_artists.each do |artist| %>
        <p class="list-side"><%= artist %></p>
      <% end %>

    </div> -->


<ul class="nav nav-pills mb-3" id="pills-tab" role="tablist">
  <li class="nav-item" role="presentation">
    <button class="nav-link active" id="pills-home-tab" data-bs-toggle="pill" data-bs-target="#pills-home" type="button" role="tab" aria-controls="pills-home" aria-selected="true">My playlists</button>
  </li>
  <li class="nav-item" role="presentation">
    <button class="nav-link" id="pills-profile-tab" data-bs-toggle="pill" data-bs-target="#pills-profile" type="button" role="tab" aria-controls="pills-profile" aria-selected="false">My top artists</button>
  </li>


</ul>
<div class="tab-content" id="pills-tabContent">
  <% @top_artists.each do |artist| %>
  <div class="tab-pane fade show active" id="pills-home" role="tabpanel" aria-labelledby="pills-home-tab"><%= artist %></div><% end %>
  <% @playlists.each do |playlist| %>
  <div class="tab-pane fade" id="pills-profile" role="tabpanel" aria-labelledby="pills-profile-tab"><%= artist %></div><% end %>
</div>
